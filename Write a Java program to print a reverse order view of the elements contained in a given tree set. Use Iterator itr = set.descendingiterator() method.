import java.util.*;
public class Code10 {
    public static void main(String args[]) {
        TreeSet<String> set = new TreeSet<>();
        Collections.addAll(set, "surya", "messi", "neymar", "ronaldo", "foden");
        Iterator<String> itr = set.descendingIterator();
        while (itr.hasNext()) {
            System.out.println(itr.next());
        }
    }
}
